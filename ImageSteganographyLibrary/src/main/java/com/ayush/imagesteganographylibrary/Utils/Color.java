package com.ayush.imagesteganographylibrary.Utils;

import android.support.graphics.drawable.ArgbEvaluator;
import com.ayush.imagesteganographylibrary.Utils.Hex;
import android.graphics.Color;

public class Color {

    public static int A = 1;
    public static int B = 2;
    public static int C = 3;
    public static int D = 4;
    public static int E = 5;
    public static int F = 6;

   // public int red = Integer.parseInt("");
   // public int green = Integer.parseInt("");
   // public int blue = Integer.parseInt( "");

//converts hex string letters such as A to an int
    public String convert(int hex){
        if (int hex = String){
            try {
                int hex = Integer.parseInt("");
            }
        }
    }

//take the string of hex, grab three digits if hex = 3 and any letters are present convert them into numbers
    //after conversion the function should print the 3 numbers which will later be used to create a color tile
    //to:do create another loops that repeats the loop below till we run out of hex digits
    public static int argb(int alpha, int red, int green, int blue) {
        for (int hex = 0; hex < 10; hex++){
            if(hex == 9){
                if (hex == String) {
                   convert();
                    System.out.println(hex);
                }
            }
        }
    }
}
